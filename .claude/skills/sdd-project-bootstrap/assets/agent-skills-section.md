## Agent skills

### Issue tracker

TODO: descrever onde as issues deste projeto são rastreadas (ex.: GitHub Issues via `gh` CLI) e criar `docs/agents/issue-tracker.md` com o detalhe, se aplicável.

### Domain docs

Domain documentation lives under `.specs/` (not root `CONTEXT.md`/`docs/adr/`) — project-level specs, codebase docs, and per-feature specs. Criar `docs/agents/domain.md` descrevendo o layout específico deste projeto (copiar de outro repo com `.specs/` como ponto de partida).

### Fluxo de desenvolvimento com IA

Sequência padrão (plugins/skills obrigatórios, ordem SDD → requisitos → grilling/brainstorming → implementação, defaults de stack). See `docs/agents/fluxo-ia.md`.
