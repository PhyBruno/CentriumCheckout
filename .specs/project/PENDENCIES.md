# Pendências e Edge Cases — Mapa Consolidado

Índice de toda pendência real aberta no projeto (dúvida de requisito, edge case não confirmado, bloqueio deliberado, trabalho de design ainda não feito). **Não é a fonte da verdade** — cada item continua documentado por completo no arquivo de origem; aqui só há um resumo de uma linha + link, para não duplicar texto que fica desatualizado. Ao resolver um item, atualize o documento de origem primeiro (e a tabela de Requirement Traceability da feature, se aplicável) — só depois marque como resolvido aqui.

**Última atualização:** 2026-08-31 (AD-109 a AD-113) — **item 36 fechado por completo, e a seção 1 fica sem nenhuma pendência aberta com a equipe do ERP**: a nova feature 014 (validação prévia da venda via `ValidarNFCe`, gate de **inserção de pagamento**) dá destino às três mudanças de contrato que restavam no item — `ValidarNFCe` vira feature própria em vez de emenda à 008 (AD-109), `FormaEntrada` é insumo obrigatório da regra de crediário do próprio endpoint (AD-111) e `FormaFpgUtiCar` já era sem ação (confirma AD-048). Nenhuma das três dependia de resposta do ERP. Ver `.specs/project/STATE.md` (AD-109 a AD-113) e `specs/014-validacao-previa-nfce/`. Atualização anterior: 2026-08-31 (AD-108) — **item 31 fechado e sub-pendência `ListaPrecoDefault` do item 36 resolvida**: decisão direta do usuário — quando a venda corre com o **cliente default**, o Checkout usa `SessaoUsuario.ListaPrecoDefault` (`GetSessao`) como lista de preço desse cliente, **não** chama `GetCliente` e trata o desconto de convênio como inexistente (`descontoConvenio = 0`). Isso elimina a lacuna que a AD-094 havia registrado como bloqueante para `TipoPreco = 9` no cenário padrão de AD-032, e deixa **três** mudanças abertas no item 36. Ver `.specs/project/STATE.md` (AD-108). Atualização anterior: 2026-08-31 (AD-107) — **sub-pendência `DavNum` do item 36 resolvida**: confirmação direta do usuário de que o próprio ERP identifica se a NFCe faturada veio de um DAV, portanto o Checkout não informa o DAV em `CheckoutFaturarNFCe` (mesma mecânica de AD-058). A feature 006 fica **desbloqueada** e seus artefatos deixaram de modelar `DavNum`; as outras quatro mudanças de contrato do item 36 seguiam abertas naquele momento (hoje são três — ver a atualização acima). Anterior: 2026-08-27 (AD-102) — **item 33 resolvido**: o usuário forneceu diretamente a lista completa e definitiva dos dez literais reais de `StatusPIXOutput.StatusTransacao` (`'C'` Criada, `'A'` Aberta, `'G'` Aguardando Pagamento — pendente; `'P'` Pagamento Recebido, `'M'` Pagamento Liberado Manualmente — aprovado, ambos; `'X'` Expirada, `'R'` Recusada, `'E'` Erro, `'F'` Fechada, `'O'` Removido Associação PIX — falha terminal, reaproveitando o fluxo de fechamento manual já decidido em AD-040), corrigindo a leitura inicial da fase Design da feature 009 (que só tinha os *nomes* de cinco estados via KB GeneXus, sem os literais nem quatro estados adicionais). Ver `.specs/project/STATE.md`. Atualização anterior: 2026-08-27 (AD-101) — **item 32 resolvido**: nova inspeção direta do código-fonte real de `PCheckout_ValidaTicketDevolucao` na KB GeneXus (`CentriumDEVU6`, via MCP) confirma que o procedure atribui `&Valido = true`/`false` explicitamente nos dois ramos (`if`/`else`) antes de retornar — o campo é preenchido de fato, sem depender de resposta da equipe do ERP. Corrige AD-099 (que fixava um fallback obrigatório para `Mensagem` por falta dessa confirmação): `interpretarRespostaTicket` passa a usar só `Valido`. Ver `.specs/project/STATE.md`. Atualização anterior: 2026-08-27 (AD-100) — **item 33 aberto**: a fase Design (`/speckit-plan`) da feature `009-pagamento-pix` confirmou, lendo o código-fonte real do ERP via KB GeneXus (`PCheckout_StatusPIX`/`PTransacao_CentriumPag_GetStatusPAG`), os cinco nomes de estado do domain `StatusTransacao` (`Aguardando`, `PagamentoRecebido`, `Expirada`, `Recusada`, `Erro`) mais o literal inicial `'G'` — mas não os literais exatos armazenados por cada nome, que a ferramenta de introspecção de domain disponível não expôs. Não abriu pendência nova para os dados do pagador de `GerarPIX` (**AD-100**, decisão direta do usuário: cliente identificado ou default da venda; e-mail/telefone sempre vazios — gap de escopo do Checkout, não depende do ERP). Ver `.specs/project/STATE.md`. Atualização anterior: 2026-08-26 (AD-097/AD-098/AD-099) — **item 32 aberto**: a fase Design (`/speckit-plan`) da feature `008-pagamento-geral` encontrou que `ValidaTicketDevolucaoOutput` **tem** um campo `Valido: boolean` no contrato, contradizendo AD-023 (que afirmava não existir campo booleano de validade e fixava a comparação de `Mensagem` ao literal `'Ticket Válido'`) — falta o ERP confirmar se `PCheckout_ValidaTicketDevolucao` efetivamente preenche o campo; até lá vale AD-099 (`Valido` com fallback para `Mensagem`). Os outros dois achados da mesma fase **não** abriram pendência: **AD-097** — não existe endpoint de formas/condições de pagamento, o catálogo vem de `SessaoUsuario.CondicoesDePagamento[]` em `GetSessao` (correção de redação de `PAY-01`); **AD-098** — rateio do desconto de capa fixado como divisão igual com clamp e redistribuição, por decisão direta do usuário. Ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-094) — **item 31 aberto**: a fase Design (`/speckit-plan`) da feature `005-identificacao-cadastro-cliente` encontrou que `GetCliente` só aceita `CPFCNPJ`, sem forma de buscar o cliente default (`GetSessao.ClienteDefaultCodigo`) por código — bloqueia `TipoPreco=9` sem interação do operador com o modal de busca. Ver `.specs/project/STATE.md` (AD-094). Atualização anterior: mesmo dia (AD-093) — nenhum item novo aberto (resolvido por remoção de escopo, não por pendência de ERP): filtro "Ativo" removido do modal de busca de cliente por falta de campo de status no contrato. Atualização anterior: mesmo dia (AD-091/AD-092) — nenhum item novo aberto e nenhum removido. A fase Design (`/speckit-plan`) da feature `003-carrinho-produto-precificacao` levantou dois achados de contrato ao inspecionar o `ApiCentriumOAuth.yaml`, e ambos foram fechados por decisão direta do usuário no mesmo dia, sem virarem pendência com a equipe do ERP: **AD-091** — `GetListaProdutos` não devolve `PrecoVenda` nem `ProdutoPesavelEditavel` e não aceita `Tipopreco`/`Codcliente`/`Listapreco`, logo o modal de lista só capta/seleciona produtos e a linha do carrinho é sempre resolvida por `GetProduto`; **AD-092** — não existe lista de preço padrão da empresa, `TipoPreco = 9` usa sempre a lista do cliente, e o campo `SessaoUsuario.listaPrecoPadrao` citado em redações anteriores de `CONCERNS.md`/AD-025 nunca existiu no contrato. Ver `.specs/project/STATE.md` e `.specs/features/carrinho-produto-precificacao/spec.md`. Atualização anterior: mesmo dia (AD-089) — item 18 **removido da tabela** (resolvido por completo): decisão direta do usuário confirma os gatilhos de UI de finalização/suspensão — desktop com botões dedicados "Cancelar Venda" (suspende) e "Finalizar Venda" (fatura) na própria tela principal, sem modal; mobile com ícone de lixeira no menu superior direito (presente nas 3 telas do wizard, não só na etapa final) para suspender e botão "Finalizar Venda" na etapa 03; ver Notas, `.specs/project/STATE.md` e `.specs/features/finalizacao-suspensao-venda/spec.md`. Atualização anterior: mesmo dia (AD-088) — item 7 **removido da tabela** (resolvido por completo): decisão direta do usuário confirma que `GetStatusSistema` já implementa a semântica de limiar decidida em AD-075/AD-080 e que o parâmetro `Cadmaqcod` deve ser enviado com o valor de `SessaoUsuario.CadMaqCod` (recebido em `GetSessao`); ver Notas e `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-087) — itens 10 e 24 **removidos da tabela** (resolvidos por completo): usuário atualizou `ApiCentriumOAuth.yaml` (`info.version: 20260826163735`) entregando os dois compromissos assumidos pela equipe do ERP em AD-081 — `ListaDAVs` ganhou `Datainicial`/`Datafinal` (item 10) e `GerarPIXOutput` ganhou `Trnbase64image` (item 24); ver Notas e `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-084) — item 20 **permanece na tabela**, mas com revisão de segurança concluída: conteúdo bruto de `SKILL.md`/`CLAUDE.md` de `goey-toast`/`boneyard` revisado via GitHub API (sem instalar nada) — nenhum conteúdo malicioso encontrado, nenhum script `postinstall`/`preinstall` automático nos dois pacotes; ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`. Atualização anterior: mesmo dia (AD-083) — item 22 **removido da tabela** (resolvido por completo): usuário forneceu o código-fonte real do `Impressao.js` do PDV atual, confirmando o contrato técnico do serviço de impressão local (host/porta via `CadMaqHost`, default `127.0.0.1:4545`, `POST` para a raiz, `text/plain`, corpo = `XMLImpressao` cru, sem validação de resposta) — resolve sem depender de nenhuma resposta da equipe do ERP; ver Notas e `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-082) — metade do item 22 resolvida (indicativo `GetSessao.TipoImpressao`, `'E'`/`'P'`). Atualização anterior: mesmo dia (AD-075 a AD-081) — sete itens resolvidos/atualizados a partir de decisões do usuário, várias confirmadas por inspeção direta da KB real do GeneXus (`CentriumDEVU6`, via MCP): item 7 **permanece na tabela** — semântica de retorno decidida (`numeric` com limiar, `0` = nada mudou, `>=1` = mudou desde a última captura; AD-075, **corrigido no mesmo dia por AD-080**, que trocou a semântica de `boolean` para `numeric`/limiar), mas o endpoint ainda não implementa isso (`PCheckout_GetStatusSistema` retorna `CadStatus` bruto hoje), então segue bloqueado pelo ERP; item 23 **removido da tabela** (resolvido — polling de 60s só entre vendas, decisão que não depende de nenhuma mudança no ERP, comportamento 100% do lado do Checkout, ver Notas); item 29 **removido da tabela** (resolvido, deixou de ser bloqueante — parse completo do código de barras de balança + fórmula de quantidade confirmados em `PAnalisaCodigoProduto`/`WWPNFCE`, AD-076, ver Notas); item 10 com decisão de produto tomada, mas permanece na tabela — endpoint ainda pendente do ERP (filtro de data real no modal de DAVs, AD-077); item 30 **removido da tabela** (resolvido — `FormaIntegracaoCartao` confirmado em `GetSessao`, corrige AD-073, AD-078, ver Notas); item 24 permanece na tabela — achado que corrige a suposição de baixa confiança de AD-047 (a imagem do QR Code já é gerada/persistida pelo ERP, só falta expor no contrato de `GerarPIX`, AD-079), endpoint ainda pendente; e confirmação de que a equipe do ERP vai atualizar os endpoints pendentes dos itens 10 e 24 (AD-081) — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-067 a AD-073, sessão de grilling de auditoria de `.specs/`) — item 29 adicionado: parse fino do código de barras pesável reclassificado como pendência **bloqueante** (corrige AD-028, que tratava como detalhe não-bloqueante); item 30 adicionado: distinção "cartão avulso" no roteamento de TEF registrada como pendência **não-bloqueante**, `PAY-08` mantém a regra atual; nenhum item novo necessário para `FaturarNFCe.produtos`/tier de preço (satisfeito pelo log de auditoria, AD-069) nem para `DavMatProdPes` (mesma regra de linha congelada, AD-069) — ver `.specs/project/STATE.md`. Atualização anterior: 2026-08-25 (AD-066) — item 28 adicionado: display secundário (tela do cliente) confirmado como gap real de escopo, sem decisão de incluir/excluir ainda, requer UI no Pencil e fase Specify dedicada; ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-063) — item 4 resolvido: campo `SDTCheckout_GetProduto.ProdutoPesavelEditavel` confirmado pelo usuário, resolve simultaneamente a flag de editabilidade ao TAB e a semântica de produto pesável; ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`. Atualização anterior: mesmo dia (AD-061/AD-062) — nova feature `auditoria-acoes-operador` (AD-061), campo `Log` confirmado existente em `CheckoutFaturarNFCe`; AD-062 remove a referência a `produtoCancelado` dos itens 6/21 (superado pelo log de auditoria); item 26 resolvido (yaml já traz `GetDavOutput` correto). **Correção (2026-08-25):** um item 27 chegou a ser registrado aqui alegando chave `CheckoutFaturarNFCe` duplicada no yaml — engano de leitura da IA, removido; existe só uma definição do schema, com `Log`/`DavNum` presentes. Atualização anterior: mesmo dia (AD-058) — item 13 resolvido: confirmado por decisão direta do usuário que o próprio ERP fecha a DAV automaticamente ao identificar, no faturamento via `FaturarNFCe`, que o rascunho de NFCe (gerado por `GetDAV`, AD-057) está vinculado à DAV de origem — sem exigir campo novo em `CheckoutFaturarNFCe`; ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-057) — item 26 adicionado: `ApiCentriumOAuth.yaml` está desatualizado quanto a `GetDavOutput` (ainda documenta `SDTDav`, mas o ERP devolve o mesmo shape de `CarregarNFCe` ao gerar automaticamente um rascunho de NFCe em `GetDAV`) — usuário confirmou que vai atualizar o yaml; ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`. Atualização anterior: mesmo dia (AD-036 a AD-056) — sessão de grilling (`.specs/project/DECISIONS.md`) materializada em 21 novos ADs. Quatro itens novos adicionados a este mapa: TEF (protocolo/mecanismo/timeout, bloqueio deliberado — parceiro será trocado, AD-037), contrato técnico completo do serviço de impressão local + indicativo faltante no `GetSessao` de qual mecanismo de impressão usar (AD-037), timing de chamada de `GetStatusSistema` no fluxo (distinto da pendência já existente sobre semântica dos códigos, AD-051) e incerteza reconhecida sobre `UtilizaEncurtador`/`UtilizaLinkExterno` (AD-047) — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-035) — item 11 (ação de reimpressão por linha no Modal DAV) resolvido por decisão direta do usuário — não será implementada pelo Checkout; reimpressão de DAV é ação que o operador deve fazer pelo próprio ERP — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-032) — item 8 (comportamento quando `GetListaVendedores` retorna vazia) resolvido por decisão direta do usuário — vendedor e cliente passam a ter default pré-selecionado (`SessaoUsuario.VendedorCodigo`/`VendedorNome` e `ClienteDefaultCodigo`/`ClienteDefaultNome`, via `GetSessao`) desde o início da NFCe, então nunca existe estado "sem vendedor selecionado" a bloquear — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-031, corrigido no mesmo dia por AD-060) — item 1 (semântica de `TipoPreco` = `6`, `7`, `10`, `11`) resolvido por decisão direta do usuário — os quatro casos foram identificados e estão no escopo do Checkout, tratados via o campo `PrecoVenda` (mesma regra dos demais casos não-`8`), sem lógica especial — ver `.specs/project/STATE.md`. Atualização anterior: 2026-08-24 (AD-030) — itens 14 e 15 (`CART-09`/`CART-10`, bloqueios deliberados do usuário) resolvidos por decisão direta do usuário — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-028) — item 3 (formato de código de barras pesável) resolvido por decisão direta do usuário — ver `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-027) — item 4 (comportamento de TAB na grid / editabilidade de produto) estreitado de "comportamento de UI não desenhado" para pergunta de contrato/KB, por decisão direta do usuário. Atualização anterior: mesmo dia (AD-026) — itens 5 (intervalo de polling de `StatusPIX`), 6 (trilha de auditoria de cancelamento em `FaturarNFCe`), 9 (campos "Limite de crédito"/"Permite venda a crédito") e 12 (URL do "Relatório de resumo de caixa") resolvidos por decisão direta do usuário; item 21 (campo de autoria de cancelamento) resolvido pela mesma decisão do item 6 — ver Notas e `.specs/project/STATE.md`. Atualização anterior: mesmo dia (AD-025), item 2 (`usaPrecoPorQuantidade`) resolvido e item 1 (`TipoPreco`/`ListaPreco`) corrigido e estreitado, a partir de regra de negócio de `TipoPreco`/`EmpDefPre` confirmada diretamente pelo usuário (ver `.specs/codebase/CONCERNS.md`). Atualização anterior: mesmo dia, item 21 adicionado (campo de autoria de cancelamento no SDT de produto). Atualização anterior: mesmo dia, item 16 resolvido pelo frame "PDV Online Web - Skeleton Carregamento" desenhado no Pencil (ver Notas). Atualização anterior: 2026-08-21, após AD-024 (`.specs/project/STATE.md`) — verificação direta na KB real do GenExus (não só no arquivo de contrato, como em AD-023) para as pendências remanescentes.

---

## 1. Pendências de confirmação com a equipe do ERP

Dúvidas operacionais reais — não são ambiguidade de produto, dependem de resposta da equipe do ERP (ou, em alguns casos, de nova inspeção do KB GenExus).

_Nenhum item nesta categoria no momento — o **item 36 foi fechado em 2026-08-31** (AD-109/AD-111), ver Notas._

### Histórico do item 36 (fechado em 2026-08-31)

| # | Item | Onde mora | Nota |
|---|---|---|---|
| ~~36~~ | ~~Três mudanças do contrato `ApiCentriumOAuth.yaml` (`20260826163735` → `20260827192357`) ainda não refletidas nas features às quais pertencem: novo endpoint `ValidarNFCe`; `FormaEntrada` (`FpgEnt`) em `CondicaoFormasDePagamento[]`; confirmação de que `FormaFpgUtiCar` segue não preenchido no branch de fallback de `PCheckout_GetSessao`~~ | `specs/014-validacao-previa-nfce/research.md`, "Achados de contrato desta fase" | **FECHADO (2026-08-31, AD-109/AD-111):** as três mudanças restantes foram tratadas. `ValidarNFCe` deixou de ter destino provisório "feature 004" e virou a **feature própria 014** — é gate de **inserção de pagamento**, não de finalização (AD-109). `FormaEntrada` foi resolvido como **insumo obrigatório** da regra de crediário do próprio `ValidarNFCe` (`FormaFpgUtiCar = 'CRD'` **e** `FormaEntrada = 'N'`), o que obriga a feature 008 a carregá-lo do catálogo e enviá-lo em cada forma do retrato (AD-111). `FormaFpgUtiCar` já era registrado como "sem ação" — apenas confirma a ressalva de AD-048. Nenhuma das três depende de resposta da equipe do ERP. Histórico anterior: **Atualizado (2026-08-31, AD-108):** `ListaPrecoDefault` **saiu deste item, resolvida** — o campo é a lista de preço do **cliente default** (o `CliListCod` dele, com fallback `1` aplicado pelo próprio ERP) e passa a ser a fonte do parâmetro `Listapreco` de `GetProduto` quando `TipoPreco = 9` e a venda corre com o cliente default, o que também **fecha o item 31** (ver AD-108 e `specs/005-identificacao-cadastro-cliente/research.md`, D3/D10). Antes disso, em **AD-107**, a remoção de `DavNum` de `CheckoutFaturarNFCe` também saiu deste item — o ERP identifica sozinho que a NFCe faturada veio de um DAV (mesma mecânica de AD-058), e a feature 006 está **desbloqueada**. Seguem abertas as três restantes, levantadas durante o Design da feature 013 e **fora do escopo dela**. Destinos: `ValidarNFCe` → feature 004; `FormaEntrada` → feature 008; `FormaFpgUtiCar` → confirma a ressalva de AD-048, sem ação |

## 2. Pendências de implementação já entendidas

Mecanismo geral já confirmado — falta só decidir/nomear um detalhe técnico específico, não é mais dúvida de "isso existe?".

_Nenhum item nesta categoria no momento — item 13 resolvido em 2026-08-25 (AD-058), ver Notas._

## 3. Bloqueios deliberados do usuário (não resolver ainda)

Diferente das seções acima — aqui a ambiguidade **não** é falta de resposta do ERP, é decisão explícita do usuário de não avançar por ora.

| # | Item | Onde mora | Nota |
|---|---|---|---|
| 25 | TEF — protocolo de comunicação, mecanismo de invocação, timeout/erro | `.specs/features/pagamento-tef/spec.md`, Edge Cases | **Novo (2026-08-25, AD-037):** bloqueio deliberado do usuário — o parceiro de TEF será trocado, não vale desenhar contrato para o parceiro atual |

_Itens 14 e 15 resolvidos em 2026-08-24 (AD-030), ver Notas._

## 4. Pendências de design (visual/técnico)

Não são dúvida de requisito — é trabalho de design ainda não feito, ou uma checagem visual pendente.

| # | Item | Onde mora | Nota |
|---|---|---|---|
| 17 | `layout-responsivo-mobile` — fase Design técnico não iniciada | `.specs/features/layout-responsivo-mobile/spec.md`, Requirement Traceability (`MOB-01` a `MOB-05`) | Design visual 100% concluído; falta breakpoint, componentes React, hook `useIsMobile` |
| 28 | Display secundário (segunda tela voltada ao cliente — propaganda, QR Code do PIX, agradecimento) | `.specs/project/STATE.md`, AD-066 | **Gap real de escopo, confirmado pelo usuário (2026-08-25):** ausente de `PROJECT.md`/`ARCHITECTURE.md`/`ROADMAP.md`. Precisa de UI própria no Pencil e de uma fase Specify dedicada antes de entrar no roadmap — ver fluxograma "Tela do cliente" em `Fluxograma - Diagrama - Alinhamentos\FLUXOS-MERMAID.md` |

## 5. Infraestrutura/deploy

| # | Item | Onde mora | Nota |
|---|---|---|---|
| 19 | Orquestração além de `docker-compose` simples (Kubernetes?) | `.specs/codebase/CONCERNS.md`, seção Docker | Não bloqueia desenvolvimento de features — decidir na primeira sprint de infra |

## 6. Riscos/lembretes de segurança

| # | Item | Onde mora | Nota |
|---|---|---|---|
| 20 | `goey-toast`/`boneyard` embutem `SKILL.md`/`CLAUDE.md` voltados a agentes de IA | `.specs/codebase/CONCERNS.md` | **Revisão do conteúdo bruto concluída (2026-08-26, AD-084):** nenhum conteúdo malicioso encontrado nos dois repositórios (sem exfiltração, sem comando destrutivo, sem `postinstall`/`preinstall` automático). Item **permanece na tabela**: o lembrete de reler o conteúdo bruto no momento da instalação real (ainda pendente do scaffold) segue valendo, pois os pacotes podem publicar versões novas até lá |

---

## Notas

- Item 34 (formato delimitado de `CenarioPagamento`) removido em 2026-08-31: decisão direta do usuário — **serialização estruturada não é viável no ERP**, mas fica garantido que os campos de texto do cadastro de cenários (`CPgFpgDes`, `CPgPraDes`, `CPgNome`) **não conterão o separador `;`**. Com isso o item deixa de ser pendência: o formato atual é definitivo e não ambíguo na prática. O descarte de itens com número de campos diferente de 7 (AD-105) **permanece implementado**, agora como defesa pura contra dado inesperado — não se espera que ele descarte nenhum cenário legítimo. Ver `.specs/project/STATE.md` (AD-105) e `specs/013-venda-rapida-cenario-pagamento/contracts/erp-cenario-pagamento-api.md`.
- Item 35 (literal de `CPgIsEncerraOperacao`) removido em 2026-08-31: decisão direta do usuário — o **conjunto fechado de literais aceitos é a solução definitiva**, não uma tolerância provisória à espera do literal exato. O parser continua aceitando `true`/`1`/`s`/`sim`/`y`/`yes` (sem distinção de caixa, após `trim`) e tratando qualquer outro valor como `false` (fail-safe: na dúvida não finaliza a venda sozinho). Não há mais nada a confirmar com o ERP neste ponto. Ver `.specs/project/STATE.md` (AD-106).
- `.specs/codebase/CONVENTIONS.md`, `STRUCTURE.md` e `TESTING.md` (padrão brownfield mapping) **não** são pendência real — dependem de código existir, e serão gerados assim que o scaffolding inicial existir (ver `.specs/codebase/CONCERNS.md`).
- Itens resolvidos por `AD-023` e `AD-024` (`.specs/project/STATE.md`) já foram removidos deste mapa (numeração renumerada em 2026-08-21 após a remoção de AD-024); o histórico completo de cada rodada de resolução fica lá, não aqui.
- Item 16 (`AUTH-05` sem frame dedicado no Pencil) removido em 2026-08-24: o frame "PDV Online Web - Skeleton Carregamento" (id `BIu92`) foi desenhado em `design/CentriumCheckout.pen`, resolvendo a lacuna de mockup — ver UI Design em `.specs/features/autenticacao-sessao-bootstrap/spec.md`.
- Item 2 (`usaPrecoPorQuantidade` — nome real do campo) removido em 2026-08-24: resposta direta do usuário confirma que não existe flag booleano separado no contrato — o modo "preço por faixa de quantidade" é indicado pelo próprio `SessaoUsuario.TipoPreco = 8` (mesma correção que estreitou o item 1) — ver `.specs/codebase/CONCERNS.md` e `.specs/features/carrinho-produto-precificacao/spec.md`.
- Item 5 (intervalo de polling de `StatusPIX`) removido em 2026-08-24 (AD-026): decisão direta do usuário — a cada 10 segundos, sem estratégia de backoff — ver `.specs/features/pagamento-pix/spec.md`.
- Itens 6 e 21 (trilha de auditoria de cancelamento em `FaturarNFCe` / campo de autoria de cancelamento no SDT de produto) removidos em 2026-08-24 (AD-026). **Mecanismo corrigido em 2026-08-25 (AD-062):** a solução original de 2026-08-24 (campo dedicado `produtoCancelado`, `boolean`, no SDT `CheckoutFaturarNFCe`) NÃO é implementada — cancelamento de item é rastreado só pelo evento `PRODUTO_CANCELADO` no log de auditoria geral da venda (`.specs/features/auditoria-acoes-operador/spec.md`), sem campo dedicado no SDT de produto. Ver `.specs/features/finalizacao-suspensao-venda/spec.md` e `.specs/features/carrinho-produto-precificacao/spec.md` para o comportamento vigente.
- Item 26 (`GetDavOutput` desatualizado no yaml — documentava `SDTDav` em vez do shape de `CarregarNFCe`) removido em 2026-08-25: confirmado no `APICentriumOAuth.yaml` mais recente (`info.version: 20260825172440`) que `GetDavOutput` já referencia `CheckoutFaturarNFCe` corretamente — usuário já atualizou o contrato conforme prometido em AD-057. Ver `.specs/codebase/CONCERNS.md`.
- Item 4 (flag de editabilidade de produto ao TAB) removido em 2026-08-25 (AD-063): campo real identificado — `SDTCheckout_GetProduto.ProdutoPesavelEditavel` (string), confirmado pelo usuário com os valores `'S'` (pesável, etiqueta), `'B'` (pesável, balança), `''` (não pesável, não editável) e `'E'` (não pesável, editável). O mesmo campo também esclarece a semântica de "produto pesável" (`ProdutoPesavel`/`MatProdPes`), antes sem enum confirmado. Ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`.
- Item 9 (campos "Limite de crédito"/"Permite venda a crédito" no cadastro simplificado) removido em 2026-08-24 (AD-026): decisão direta do usuário — remover os dois campos da tela. Remoção visual no frame do Pencil (`PDV Online Web - Modal cadastro de cliente`) ainda não aplicada — ver `.specs/features/identificacao-cadastro-cliente/spec.md`.
- Item 12 (URL da opção "Relatório de resumo de caixa") removido em 2026-08-24 (AD-026): decisão direta do usuário — mesmo link da opção "Central de movimentação não fiscal" (`WPMovimentoNaoFiscal_Lancamento.aspx`) — ver `.specs/codebase/ARCHITECTURE.md`, seção Responsividade.
- Item 3 (formato de código de barras pesável — detecção) removido em 2026-08-24 (AD-028): decisão direta do usuário — código de barras com 13 dígitos, começando em `2`, indica produto gerado por balança (pesável). Confirma o padrão EAN-13 de balança (descarta a hipótese de sintaxe `código*quantidade`). **A extração fina dos demais dígitos (código do produto vs. peso/valor vs. dígito verificador) voltou como item 29 em 2026-08-26 (AD-068), agora marcada bloqueante — a redação original deste item, que tratava a extração fina como "detalhe de implementação a confirmar na fase Design", subestimava o impacto: sem a máscara completa, a story de inserção direta não é implementável para produtos pesáveis.** Ver `.specs/codebase/CONCERNS.md` e `.specs/features/carrinho-produto-precificacao/spec.md`.
- Itens 14 e 15 (`CART-09`/`CART-10`, bloqueios deliberados) removidos em 2026-08-24 (AD-030): decisão direta do usuário. `CART-09` — qualquer pagamento aprovado bloqueia edição/cancelamento de item; se o pagamento aprovado for TEF ou PIX o bloqueio é permanente (sem fluxo de cancelamento dessas transações); se for cartão fora do TEF ou dinheiro, remover o pagamento reabilita a edição. `CART-10` — o Checkout não valida saldo/estoque na inserção de produto, é regra de controle do próprio ERP — ver `.specs/features/carrinho-produto-precificacao/spec.md` (Edge Cases + Requirement Traceability).
- Item 1 (semântica de `TipoPreco` = `6`, `7`, `10`, `11`) removido em 2026-08-25 (AD-031, corrigido no mesmo dia por AD-060): decisão direta do usuário — `6` = Preço de Custo, `7` = Preço da última venda, `10` = Preço Cliente x Produto (`PRM0241`), `11` = Preço por Índice. Semântica conhecida e os quatro valores **estão no escopo do Checkout** — o cálculo é feito no backend (ERP), que devolve o valor final no mesmo campo `PrecoVenda` usado pelos demais casos não-`8`; não há tratamento especial para nenhum deles (motor de precificação cobre `1`-`11` uniformemente, `8` como única exceção) — ver `.specs/project/STATE.md` (AD-060) e `.specs/codebase/CONCERNS.md`.
- Item 8 (comportamento quando `GetListaVendedores` retorna vazia) removido em 2026-08-25 (AD-032): decisão direta do usuário — vendedor e cliente passam a ter default pré-selecionado ao iniciar a NFCe (`SessaoUsuario.VendedorCodigo`/`VendedorNome` e `ClienteDefaultCodigo`/`ClienteDefaultNome`, via `GetSessao`), então nunca existe estado "sem vendedor" a bloquear — se a listagem do modal vier vazia, o operador simplesmente fecha o modal e o default já selecionado permanece válido (mesmo comportamento do edge case já previsto de fechar sem selecionar) — ver `.specs/project/STATE.md` e `.specs/features/selecao-vendedor/spec.md`.
- Item 11 (ação de reimpressão por linha no Modal DAV) removido em 2026-08-25 (AD-035): decisão direta do usuário — a reimpressão de DAV **não será implementada** pelo Checkout; se o operador precisar reimprimir, deve fazer isso diretamente pelo ERP. Botão presente no design do Pencil (`PDV Online Web - Modal DAV`) deve ser desconsiderado na implementação — ver `.specs/project/STATE.md` e `.specs/features/importacao-dav/spec.md` (UI Design, Out of Scope, Edge Cases).
- Itens 22, 23, 24 e 25 adicionados em 2026-08-25 (AD-036 a AD-056, sessão de grilling completa registrada em `.specs/project/DECISIONS.md`): serviço de impressão local (contrato técnico + indicativo faltante no `GetSessao`, AD-037), timing de `GetStatusSistema` (AD-051), incerteza sobre `UtilizaEncurtador`/`UtilizaLinkExterno` (AD-047) e TEF como bloqueio deliberado (AD-037, movido para a seção 3). A mesma rodada resolveu, sem deixar pendência nova em aberto, split de pagamento/troco (AD-036), desconto manual e arredondamento monetário generalizado (AD-039), falha de rede em `FaturarNFCe` (AD-038), fechamento de modal PIX pendente e falha em `GerarPIX` (AD-040), recuperação de NFCe como nova feature (AD-041), suspensão com pagamento aprovado (AD-042), troca de cliente/vendedor com carrinho populado (AD-043), aviso de renovação de token falha (AD-044), isolamento de tenant no Dexie (AD-045), escopo mobile (AD-046), `ConfiguracoesPIX`/`TrnTempoExpiracaoPIX` (AD-047, exceto o item 24 acima), `FormaFpgUtiCar` vazio (AD-048), falha não-401 no bootstrap (AD-049), `CliTip='F'` fixo (AD-050), concorrência em DAV/rascunho (AD-052), defaults vazios/indicador visual/filtro Ativo (AD-053), múltiplas abas (AD-054), importação de DAV sobrescreve default (AD-055) e Fato F1 (AD-056) — ver `.specs/project/STATE.md`.
- Item 26 adicionado em 2026-08-25 (AD-057): esclarecimento direto do usuário de que `GetDAV` faz o ERP gerar automaticamente um rascunho de NFCe, devolvendo o mesmo shape JSON de `CarregarNFCe` (`OutCheckoutFaturarNFCe`/`CheckoutFaturarNFCe`) — não o `SDTDav` (`DavItemStruct`/`DavForPagamento`) hoje documentado em `ApiCentriumOAuth.yaml`. A importação de DAV passa a reusar o mesmo fluxo de import da recuperação de NFCe (`.specs/features/recuperacao-nfce/spec.md`). O yaml está desatualizado nesse ponto e será atualizado pelo usuário — até lá, item 26 permanece como pendência de contrato — ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`.
- Item 13 (campo do SDT `CheckoutFaturarNFCe` para marcar DAV como importado/faturado) removido em 2026-08-25 (AD-058): decisão/confirmação direta do usuário — o rascunho de NFCe gerado automaticamente por `GetDAV` (AD-057) já fica vinculado internamente à DAV de origem; o próprio ERP identifica esse vínculo ao faturar via `FaturarNFCe` e fecha a DAV automaticamente, sem exigir nenhum campo novo em `CheckoutFaturarNFCe`. O achado de KB de AD-024 (sem escrita vinda do Checkout em `DavDocFNum`) segue correto, só não era o caminho relevante — ver `.specs/project/STATE.md` e `.specs/codebase/CONCERNS.md`.
- Item 29 (parse fino do código de barras pesável — extração dos dígitos internos) removido em 2026-08-26 (AD-076): confirmado por inspeção direta do código-fonte real do ERP na KB do GeneXus (`CentriumDEVU6`, via MCP) — `PAnalisaCodigoProduto` extrai o código reduzido do produto das posições 2-7 do código de barras e o valor da etiqueta das posições 8-12 (÷100); a posição 13 é o dígito verificador do EAN-13 (`PEAN13`), não é dado de negócio. A quantidade é calculada em `WWPNFCE` (evento `Enter`) como `round(trunc(valorEtiqueta / PrecoVenda, 5), 3)`. Máscara completa e fórmula conhecidas — sem pendência de contrato remanescente, nada depende de resposta futura da equipe do ERP. Ver `.specs/project/STATE.md` e `.specs/features/carrinho-produto-precificacao/spec.md`.
- Item 30 (roteamento de TEF por forma de pagamento — distinção entre cartão via TEF e "cartão avulso") removido em 2026-08-26 (AD-078): confirmado por inspeção direta do código-fonte real do ERP na KB do GeneXus — o campo `FormaIntegracaoCartao` (`SDTCheckout_GetSessao` → `CondicaoFormasDePagamento`, origem `FpgNfTefPos`/domínio `NFCe_tpIntegra`) já é retornado por `GetSessao` hoje (`1` = TEF/`PagtoIntegrado`, `2` = POS). Corrige a varredura anterior (AD-073), que não tinha encontrado o campo. Nenhuma mudança de contrato necessária — o campo já existe e está disponível para o Checkout usar se a distinção por forma de pagamento individual vier a ser necessária em `PAY-08`. Ver `.specs/project/STATE.md` e `.specs/features/pagamento-geral/spec.md`.
- Item 23 (`GetStatusSistema` — timing de quando é chamado no fluxo) removido em 2026-08-26 (AD-075): decisão direta do usuário — polling a cada 60 segundos, só entre vendas (nunca com carrinho preenchido ou cliente já identificado). Diferente do item 7 (semântica dos códigos de retorno), essa decisão **não depende de nenhuma mudança no endpoint pelo ERP** — a frequência/janela de quando o Checkout chama `GetStatusSistema` é comportamento inteiramente do lado do cliente, já totalmente implementável hoje independente do que o endpoint retornar. Ver `.specs/project/STATE.md` e `.specs/features/finalizacao-suspensao-venda/spec.md`.
- Item 22 (serviço de impressão local — contrato técnico completo) removido em 2026-08-26, em duas etapas no mesmo dia: **AD-082** resolveu a metade do indicativo de mecanismo (campo `SessaoUsuario.TipoImpressao`, `'E'`=impressão direta, `'P'`=PDF, retornado por `GetSessao`); **AD-083** resolveu a outra metade (contrato técnico do serviço em si) — usuário forneceu o código-fonte real do `Impressao.js`, hoje carregado no PDV atual do ERP: `POST` para `http://{SessaoUsuario.CadMaqHost}` (raiz do host, sem rota adicional; default `127.0.0.1:4545` quando `CadMaqHost` vazio), header `Content-Type: text/plain`, corpo = `XMLImpressao` cru, sem validação de formato de resposta (sucesso = a chamada não lançar erro de rede). Resolvido sem depender de nenhuma resposta da equipe do ERP — o Checkout replica um mecanismo já em produção. Ver `.specs/project/STATE.md` e `.specs/features/finalizacao-suspensao-venda/spec.md`.
- Itens 10 e 24 removidos em 2026-08-26 (AD-087): usuário atualizou `ApiCentriumOAuth.yaml` (`info.version: 20260826163735`), entregando os dois compromissos que a equipe do ERP havia assumido em AD-081. Item 10 (filtro de data real em `ListaDAVs`): endpoint ganhou os parâmetros `Datainicial`/`Datafinal`, efetivando a decisão de produto de AD-077 — `status`/vendedor/tipo/origem seguem sem suporte, mas nunca fizeram parte do escopo prometido. Item 24 (imagem do QR Code do PIX): `GerarPIXOutput` ganhou o campo `Trnbase64image`, ao lado do `Trnbase64text` já existente, expondo a imagem que o ERP já gerava internamente (achado de AD-079). A mesma atualização do contrato também formalizou em `SessaoUsuario` o campo `TipoImpressao` já assumido por AD-082 — sem pendência associada, é só a confirmação escrita no yaml. Ver `.specs/project/STATE.md`, `.specs/features/importacao-dav/spec.md` e `.specs/features/pagamento-pix/spec.md`.
- Item 7 removido em 2026-08-26 (AD-088): decisão direta do usuário — o endpoint `GetStatusSistema` já implementa a semântica de limiar decidida em AD-075/AD-080 (`0` = nada mudou desde a última captura de `GetSessao`; `>= 1` = mudou, exige rechamar `GetSessao`, sem importar o valor exato acima de 1) e o parâmetro `Cadmaqcod` (já presente no contrato) deve ser enviado com o valor de `SessaoUsuario.CadMaqCod`, recebido em `GetSessao` — informação nova, não documentada antes. Supera os achados de KB anteriores (AD-024 e checagem de 2026-08-26), que mostravam a procedure repassando `CadStatus` bruto sem transformação. Ver `.specs/project/STATE.md`, `.specs/features/finalizacao-suspensao-venda/spec.md` e `.specs/codebase/CONCERNS.md`.
- Item 18 (frame desktop dedicado à finalização/suspensão não identificado no Pencil) removido em 2026-08-26 (AD-089): decisão direta do usuário confirma o mecanismo de UI, sem exigir frame/modal dedicado no Pencil. Desktop: botão "Cancelar Venda" (suspende, `SUSPENDER`) e botão "Finalizar Venda" (fatura, `FATURAR`), ambos dentro da tela principal (`Fundo PDV Online Web`). Mobile: ícone de lixeira no menu superior direito, presente nas 3 telas do wizard (`PDV Mobile 01`, `02` e `03`), não só na etapa final, aciona a suspensão; botão "Finalizar Venda" já previsto na etapa `PDV Mobile 03 - Revisão e Finalização`. Ver `.specs/project/STATE.md` e `.specs/features/finalizacao-suspensao-venda/spec.md`.
- Item 32 (`ValidaTicketDevolucaoOutput.Valido` — se o procedure realmente preenche o campo) removido em 2026-08-27 (AD-101): confirmado por inspeção direta do código-fonte real do ERP na KB do GeneXus (`CentriumDEVU6`, via MCP) — `PCheckout_ValidaTicketDevolucao` atribui `&Valido = false` no ramo de erro (`&retorno = 0`) e `&Valido = true` no ramo de sucesso, sempre preenchendo o campo explicitamente antes de retornar. Corrige AD-099 (que definia um fallback obrigatório para `Mensagem` por não haver essa confirmação) — o fallback é removido, `interpretarRespostaTicket` passa a usar só `Valido`. Ver `.specs/project/STATE.md` e `.specs/features/pagamento-geral/spec.md`.
- Item 33 (literais reais de `StatusPIXOutput.StatusTransacao`) removido em 2026-08-27 (AD-102): usuário forneceu diretamente a lista completa e definitiva dos dez literais — `'C'` Criada, `'A'` Aberta, `'G'` Aguardando Pagamento (pendente); `'P'` Pagamento Recebido, `'M'` Pagamento Liberado Manualmente (aprovado, ambos tratados de forma idêntica); `'X'` Expirada, `'R'` Recusada, `'E'` Erro, `'F'` Fechada, `'O'` Removido Associação PIX (falha terminal, reaproveitando o fluxo de fechamento manual já decidido em AD-040). Corrige a leitura inicial da mesma fase Design (feature 009), que via KB GeneXus só tinha confirmado os *nomes* de cinco estados, sem os literais exatos nem quatro estados adicionais (`'C'`, `'A'`, `'F'`, `'O'`). Ver `.specs/project/STATE.md` e `.specs/features/pagamento-pix/spec.md`.
